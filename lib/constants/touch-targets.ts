// Touch target configuration for mobile and tablet devices
// Ensures all interactive elements meet accessibility guidelines

export const TOUCH_TARGET = {
  minSize: 44, // pixels - minimum touch target size for buttons and clickable elements
  minSpacing: 8, // pixels - minimum spacing between interactive elements
} as const;
